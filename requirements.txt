# Runtime dependencies for platforms that install via `pip install -r requirements.txt`
# Keep this in sync with [project].dependencies in pyproject.toml.
discord.py>=2.3.0
# Install this project package (src layout) when host only runs requirements install.
.
